import WorkBlock from "@/components/pages/work/_work-block";
import { Metadata } from "next";

export const metadata: Metadata = {
  title: "Omulod ",
};

export default function WorkPage() {
  return <WorkBlock />;
}
